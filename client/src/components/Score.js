import React, { useContext } from 'react';
import globalContext from '../utils/globalContext';

function Score(){

    const {score, totalScore} = useContext(globalContext);

    return (

    );
}

export default Score;